
28.尝试重新学习字符串操作以及KMP模式匹配算法！！！(还没有完成手搓，或许暂时先搁置)

896.单调数列 中了解到，对于vector容器的反向迭代器(从尾到头遍历)的方式为：￼使用反向迭代器for(auto it=v.rbegin();it!=v.rend();it++)￼这里的rbegin()逻辑上指向原end()-1处，rend()逻辑上指向原begin()-1处，it++会导致反向迭代器自动反向迭代（故称为反向迭代器）,但是这种方式由于是使用了STL容器库中封装的方式，所以实际是很安全的！

682.棒球问题￼在这道题中学习到了C++ string中的一个方法：stoi(将string转化为对应的int,如果转化失败会抛出异常，成功则返回转化后的INT型结果),能够将正整数，0，负整数形状的string都转化为整数！

1. 螺旋矩阵￼这题和上一道题机器人脱离圈一样，属于“有限状态机”模拟的类型题，编程实现有限状态机的状态切换可以考虑利用mod模运算实现。

1. 两个超长整数相乘，太难啦！！！不会，2025/4/23学习中

对于大整数乘法的理解(数学理解)太弱了。

56.合并区间，使用sort()函数的时候遇到了问题，当调用STL中sort()函数的时候，自定义的cmp排序函数接受两个参数并返回是否需要交换顺序的bool型变量，这里的sort函数要求排序方法必须使用严格弱序方法，使用return a\>=b或者\<=的时候可能不满足严格弱序导致出现未定义错误，程序可能报错，慎重！！！(西电oj第一题估计就是这个原因导致的！)

473 路经总和III￼官方解中的第二种 前缀和方法看的不明白，有时间重看！
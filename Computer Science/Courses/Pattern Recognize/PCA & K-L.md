
PCA证明:

yi=ai * x
寻找最大var(yi) 
可证var(yi)为 ai与   $\sum$(这是x随机变量的协方差,x是高维度随机变量) 表示的变量;
进一步利用拉格朗日数乘法(要求ai是单位向量,$a_i$* $a_i^T$=1,以此杜绝方向向量长度对最后var的影响)
将$a_i$* $a_i^T$-1=0 作为condition以及var(yi)作为body构建函数,关于ai求导并需要导数为0,
可证$a_i$为$\sum$矩阵的特征向量,且对应的特征值就位var(yi); 故选取主成分通过$\sum$的特征值大小依次选取特征值作为第一,二,....主成分

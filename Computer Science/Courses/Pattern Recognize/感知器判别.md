详情见李航统计学习方法


尝试寻找二分类任务中的决策超平面；
设置以误判点到超平面距离(误判点沿着超平面法向量方向投影的距离)作为损失函数；
并非采用批量梯度下降（BGD）,而是采用随机梯度下降(SGD)，两者区别：

前者先统计所有误判样本的损失函数和，基于该和函数进行权重的梯度下降计算并迭代；
后者针对每个单独的误判样本迅速进行梯度下降计算与权重迭代；

BGD代表的梯度方向最准确，但显然计算开销大；
SGD计算时效快，当前误判立刻更新权重并用于后续其他节点的迭代，且开销小，但准确性差；